import {
  Random
} from 'mockjs';

const Data = [];

for (let i = 0; i < 86; i++) {
  Data.push({
    id: Random.guid(),
    name: Random.name(),
    createDate: Random.date(),
    order:i,
  });
}
export {
  Data
};
